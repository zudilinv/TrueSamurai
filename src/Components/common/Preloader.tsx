import React from "react";
import preloader from "../assecs/images/preloader.gif";

export const Preloader = () => {
    return (
        <div>
            <img src={preloader}/>
        </div>
    );
};

